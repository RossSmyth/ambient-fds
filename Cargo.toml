[package]
name = "ambients"
version = "0.1.0"
edition = "2024"

[dependencies]
systemd = { version = "0.10.1", optional = true }

[features]
default = [ "checked_api" ]
checked_api = [ "dep:systemd"]

[[example]]
name = "basic"
